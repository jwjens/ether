# Status — 2026-07-07, post-v4.4.37 tag

Receipts rule in force. Push state is clean (`push-state-2026-07-07.txt`: `origin/main..HEAD` empty).

---

## 1. v4.4.37 release — CI succeeded, downloadable (receipt)

`node scripts/check-release.js v4.4.37`:
- Release id `350502083`, not draft/prerelease.
- Assets `state=uploaded`: **`Ether-Setup-4.4.37.exe` (191 MB)**, `Ether-4.4.37.dmg` (229 MB), `-arm64.dmg`,
  blockmaps, `latest.yml` + `latest-mac.yml`. → installer present, downloadable under the latest release.
- **Release body is EMPTY** — needs operator notes.

**Process note (named, per the review):** the tag was cut **before** the walkthroughs, so v4.4.37 is
**compile-verified, runtime-unverified**. The tag is public — do NOT move/delete it; anything the
walkthrough breaks is fixed forward as v4.4.38.

## 2. Release notes — the "gh gap" is already solved

`scripts/post-release-notes.js` (a prior session, uncommitted) pulls the token from **`git credential
fill`** (the same credential `git push` uses) and PATCHes the release via the API — **no `gh`, no
separate token needed**. `scripts/check-release.js` (new, read-only) reuses the pattern to verify.

**Decision needed (timing):** post measured notes NOW (release is live + downloadable, empty body looks
bad) or HOLD until your walkthrough passes (avoids describing a feature that fails walkthrough)? Notes
are drafted and ready either way. Recommend: post measured/factual notes now, refine if walkthrough
changes anything.

## 3. Repo hygiene (read-only diagnosis done — cleanup PROPOSED, not executed)

| Item | Finding | Proposed |
|---|---|---|
| `$null` (tracked) | Committed at genesis `bb8e7c8` (v0.1.0); already deleted locally. PowerShell `> $null` accident. | `git rm --cached '$null'` + commit |
| `=ro` (untracked) | 0 bytes, mangled-flag accident | delete |
| `main.js` (root, untracked) | **311 KB — a copy of `electron/main.js`** (its own header says so). Stray, not the entry point. | `diff` vs `electron/main.js` to confirm it's an old/identical copy, then delete |
| `ec-deploy.sh` (untracked) | Real: `wrangler pages deploy` for ether-cast. 86 bytes. | commit as deploy tooling (or gitignore) |
| `scripts/post-notes-*.js` ×6 | Spent version-specific one-offs | delete |
| `scripts/post-release-notes.js`, `check-release.js`, `verify-push-license.js` | Reusable tooling | commit + document (this file) |

None blocks anything. Bundle into one housekeeping commit on your OK.

## 4. Walkthrough checklist — Scheduler Health panel (before/after 2ks/70s import)

Runtime verification. PASS = all boxes.
1. **Relaunch Ether** fully (main.js + renderer changed).
2. **Tools → Scheduler Health** → panel opens. **Drag the header** — confirm it moves and you can use the
   app behind it (not a locked modal).
3. **Category health tab:** every category shows songs + distinct artists + a bar. **2ks and 70s should
   read `at_risk` / `ADD SONGS`** — note their artist counts (BEFORE).
4. **Import** your 2ks + 70s songs (single-file import shipped in the 4.4.36 line).
5. **Refresh the panel (⟳)** → 2ks/70s distinct-artist counts climb → status → `tight`/`healthy` (AFTER).
6. **Calendar → Generate a day** → panel's **Last generate** tab fills: gaps show **date + hour range**,
   clicking one **jumps the calendar to that day**; the **relaxed-pick list** shows hour · song · category.
7. **After import:** the relaxed-pick list for 2ks/70s **shrinks toward empty** — the before/after proof.

## 5. Walkthrough checklist — Iris Phase 1 (badge / chat / glow / generate)

Dev, both apps running (Phase 2 bundling not built yet).
1. Launch **Iris** (separate) + **Ether**.
2. Ether shows the **Iris badge** bottom-right → goes **online** when her `:3400` stream connects.
3. Click the badge → **chat panel** opens.
4. Type a message → **her reply appears** in the panel.
5. While she speaks → **the badge glows purple** (drops when she stops).
6. "generate August for Magical Forest" → she runs Generate and **reports counts + relaxed picks + runway**.
7. Quit Iris → badge → **offline cleanly** with the how-to message (no dead end).

## 6. Suggested sequence (per the review)

1. ✅ Confirm CI/installer (done — §1).
2. Post operator notes (§2 — your timing call).
3. **Walkthroughs on OVEVENTS** with the 4.4.37 installer (or dev build) — §4, §5.
4. **Only then install 4.4.37 on jensj** (live box) — one restart picks up the never-empty selector +
   everything else; the pending 4.4.36 close-and-reopen collapses into this.
5. Housekeeping commit (§3) whenever convenient.

Track A next after this: **#3 emergency floor**. Track B next: **Phase 2 Gate 0 = §7 transport fix**
(nothing beyond until gated).
