# Ordered worklist — Jeff, 2026-08-05

**The rules for this list, from Jeff:**
- Work **IN ORDER**, one item at a time. Do not skip, reorder, or batch across items.
- Each item: **build complete → Jeff installs and verifies → THEN the next.**
- **No version bump until an item passes its acceptance. No partial commits.**
- Near context limits → **stop clean at an item boundary and say so.**
- **When an item is already diagnosed in its handoff doc, BUILD it — do NOT re-trace or re-investigate
  what is already proven.** Read-only investigation is only for the ONE item that needs it (**§4's design
  question**), not for the fixes already spec'd (**§1, §2, §3, §5**).

---

## 1 · DESKTOP BUILD — three fixes, ONE build

**a. `getSpotArt` station-scoped.** Full detail: `docs/getspotart-station-scoping-2026-08-05.md`.
`AND station_id = ?`, `ORDER BY (art_image IS NULL)` as tiebreaker **within** the station, `stationId`
threaded through `resolveArtwork` / `isSpotFile` and all 5 call sites, and **`_spotArtCache` re-keyed
`station:filePath`** (miss this and cache alone reproduces the bug).

**b. Queue-payload `content_class`.** ALREADY IN THE TREE — `src/App.tsx` staged, uncommitted. Rides
along. Backend needs no change (confirmed: `index.js:4787` whole-array `JSON.stringify`, served back
verbatim). Context: `docs/public-artwork-queue-gap-2026-08-05.md`.

**c. REMOVE the traffic-light window buttons — NEW INSTRUCTION, recorded nowhere else.**
Native title bars only. Delete:
- `src/components/WindowControls.tsx`
- its mount in `src/main.tsx` (root render + import)
- the IPC in `electron/main.js`: `win:minimize`, `win:close`, `win:toggleFullscreen`, `win:isFullscreen`
  and the `_senderWin` helper
- the `win: { … }` block in `electron/preload.js`

They were added in 4.4.142 (commit `dc2c932`) and are being reversed.

**ACCEPTANCE:** halloVeen's queue shows the spot's uploaded art **every time**; another station's art
**never** appears (give Magical Forest's row id 3 its own image and check both stations, on the deck tile
AND the queue row, including after a station switch); **no coloured dots anywhere** in any window.

---

## 2 · LISTENER — queue threading

`C:\ether-listener`. Thread `content_class` through: `QueueItem` type → `api.ts` `fetchAlbumArt(title,
artist, contentClass?)` (append `&class=`, return null immediately for JIN/SWP/SPOT) → `Fullscreen.tsx`
`NpRow` (**the queue rows in Jeff's screenshot**), `NowPlaying.tsx:23`, `EmbedWidget.tsx:31`. Deploy.

**Depends on item 1b being installed and the field visible in `/public/station/<slug>` — check that
first.**

**ACCEPTANCE (live player):** a spot in the queue shows the logo/neutral; music rows still show art; the
hero and station tiles do not regress.

**`PlayButton.tsx` stays HELD and uncommitted throughout** — see
`docs/listener-deploy-review-2026-08-05.md` for the https question that gates it.

---

## 3 · DESKTOP small batch — one build

- **JINGLES-strip ON honesty** — `App.tsx:4105-4107`: `isOn={true}` is hardcoded while `onToggleOn`
  pauses/plays the CART deck, so the control can silence carts and still render ON.
- **CARTS tab visibly alive** — currently `opacity: 0.4` with a tooltip; Jeff reports it reads as fully
  disabled.

---

## 4 · §2.1 ENGINE DEFERRAL

**Answer the design question FIRST, before building:** *is bare engine construction inert if `init()`,
`attachDaemonEvents` and the station adoption are all guarded?* If yes, the fix is guards on the effects
and the render-time `getEngine` can stay. If no, it is the lazy-engine refactor.

Everything known is in `docs/D1-launch-receipt-2026-08-05.md` §4b — including that the construction sites
(`App.tsx:533`, `AudioEngineContext.tsx:37`) run in the **component body**, before the early-return
gates, so no guard placed after the gate can stop them; and that `bootAuthDone()` in
`src/audio/boot-seq.ts` is a module-level, dependency-free auth flag better suited to gating the registry
than React state.

**ACCEPTANCE:** launch, pull `[BOOTSEQ]`, **zero PRE-AUTH lines** for engine construction / init / attach
/ adoption.

**Already settled — do not re-open:** the double sign-in (§1.1) is fixed and confirmed by log AND eye.

---

## 5 · JUKEBOX — Slice 2

Settings gear → PIN-gated settings overlay. The gear currently exits (it was relabelled ✕ in 4.4.142
precisely because there was no settings behind it). Design: `docs/jukebox-mode-design-2026-08-04.md`
§2.2, operator-role-and-above only.

---

## State at the time this list was written

- **Nothing pushed or tagged since v4.4.135.** No item below has been started.
- `C:\openair` — `src/App.tsx` + `package.json` (version string 4.4.143, provisional) **staged,
  uncommitted**. `albumArt.ts` restored — the `ORDER BY`-only fix was backed out because it leaked across
  stations. **No installer built.**
- `C:\ether-listener` — `a051d0b` deployed. `PlayButton.tsx` held.
- `C:\ether-cast` — gate deployed.
- **Deferred, acknowledged, not a bug:** uploaded spot art on public surfaces is the R2 revisit —
  `docs/spot-art-public-surfaces-2026-08-05.md`. Station logo on public spots STANDS.
