#!/bin/sh
# Pre-commit hook per [N-73]: block commits when transformer chain is incomplete.
# Uses electron (not plain node) because better-sqlite3 native binding is compiled
# against electron's Node ABI, not system Node's.

cd "$(git rev-parse --show-toplevel)" || exit 1
exec node_modules/.bin/electron --no-sandbox scripts/verify-transformer-chain.js
